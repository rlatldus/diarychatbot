import styled from 'styled-components';
import { NameCont } from '../../@shared/Name/style';

export const HeaderWrapper = styled.div`
    padding-top: 11rem;
`;

export const TitleName = styled(NameCont)`
    font-family: var(--font-notoSerifKR);
`;
